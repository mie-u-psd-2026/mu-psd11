# テスト仕様書: Flask + Ollama 文章推敲アプリ

## 1. テスト対象
- `app.py`（Flaskサーバー/APIバックエンド）
- `static/index.html`（フロントエンドUI）

## 2. テスト環境
- Windows 10/11
- Python 3.13
- Flask 3.1.3, ollama >=0.5.1（Ollama純正Python SDK）
- Ollama（ローカル起動、モデル: `qwen3.5:0.8b`、thinking対応モデル）
- ブラウザ: Chrome, Edge, Firefox

### 事前準備
- `ollama serve` が起動していること
- `ollama pull qwen3.5:0.8b` でモデルを取得済みであること
- `pip install -r requirements.txt` を実行済みであること（`ollama`パッケージを含む）

## 3. テスト項目

### 3.1. サーバー起動・静的ファイル配信

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 1  | サーバー起動 | `python app.py` で起動 | エラーなく起動し、`http://localhost:5000/` でアクセス可能 |
| 2  | index.html配信 | ブラウザで `/` にアクセス | Vue.js UI（用途選択・推敲レベル選択・入力欄・ボタン）が表示される |

### 3.2. UI操作 - 用途選択

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 3  | 大学レポート選択 | 用途プルダウンで「大学レポート」を選択し文章を送信 | 「です・ます調」等の混在がなく、レポート調に整えられた文章が返る |
| 4  | メール選択 | 用途プルダウンで「メール」を選択し文章を送信 | 相手に失礼のない、自然なメール調の文章が返る |
| 5  | 就活・ES選択 | 用途プルダウンで「就活・ES」を選択し文章を送信 | 簡潔で具体性のある、誇張を抑えた文章が返る |
| 6  | 一般文章選択 | 用途プルダウンで「一般文章」を選択し文章を送信 | 誤字脱字・不自然な表現が修正された文章が返る |

### 3.3. UI操作 - 推敲レベル選択

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 7  | 軽く修正 | 推敲レベルで「軽く修正」を選択し送信 | 原文の構成を保ったまま、誤字脱字レベルの最小限の修正のみ行われる |
| 8  | 自然に修正 | 推敲レベルで「自然に修正」を選択し送信 | 原文を活かしつつ、自然で分かりやすい文章に修正される |
| 9  | しっかり修正 | 推敲レベルで「しっかり修正」を選択し送信 | 文章構成・語順まで積極的に修正されるが、意味・事実は変わらない |

### 3.4. /send_api API 正常系

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 10 | テキスト送信 | `{"text": "...", "purpose": "report", "level": "normal"}` を送信 | 200が返り、`processed_text` に「【推敲後の文章】／【主な修正点】／【文章評価】／【総評】」の4部構成の結果が含まれる |
| 11 | purpose未指定 | `purpose` を省略して送信 | デフォルト値 `normal` として処理される |
| 12 | level未指定 | `level` を省略して送信 | デフォルト値 `normal` として処理される |

### 3.5. 出力内容の品質確認（thinkingモデル対応）

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 13 | 思考過程の非表示 | 通常通り推敲を実行 | `processed_text` に `<think>` タグやAIの思考過程（「Thinking Process」等の英語の分析文）が含まれない |
| 14 | 指示文の非復唱 | 通常通り推敲を実行 | 「〜してください」等、システムプロンプトの指示文そのものが結果に混入していない |
| 15 | 総評ラベルの表示 | 通常通り推敲を実行 | 結果末尾に「【総評】」というラベル付きでコメントが表示され、指示文がラベル代わりに出力されていない |
| 16 | 空応答時のフォールバック | （擬似的に）Ollamaが空のcontentを返す状況を再現 | `thinking`フィールドに内容があれば、「※本来の推敲結果が取得できなかったため〜」という注記付きで代替表示される。thinkingも空なら「AIから有効な応答がありませんでした。」が返る |

### 3.6. /send_api API 異常系

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 17 | 空文字送信 | `text` を空文字（または空白のみ）で送信 | 400エラー、`{"error": "文章を入力してください。"}` |
| 18 | textフィールドなし | `text` キーなしでAPI送信 | 400エラー、`{"error": "文章が送信されていません。"}` |
| 19 | Ollama未起動 | `ollama serve` を停止した状態でAPIを実行 | 500エラー、`{"error": "AIサービスとの通信中にエラーが発生しました。"}` |

### 3.7. UIテスト

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 20 | 文字数カウント | テキストエリアに入力 | 入力欄下部に「◯◯文字」が正しく表示される |
| 21 | 実行中の表示 | 「AIで推敲する」ボタン押下直後 | ボタンが非活性になり、「推敲中...」表示になる |
| 22 | 結果表示 | 応答が正常時 | 「推敲結果」見出しの下、カード内に応答内容が表示される |
| 23 | エラー表示 | エラー発生時 | 赤色のアラート欄にエラーメッセージが表示される |
| 24 | クリアボタン | 「クリア」ボタン押下 | 入力欄・結果欄・エラー欄がすべて空になる |
| 25 | 未入力での実行 | 入力欄が空の状態で「AIで推敲する」を押下 | API送信されず、「文章を入力してください。」が表示される |

## 4. その他確認事項
- 入力最大長、応答最大長の確認
- 日本語以外の入力（英語・中国語等）に対する挙動確認
- レスポンス速度の目安計測（`think=False`指定により、thinkingモード有効時より高速化されているか比較）
- `num_ctx`拡大（8192）により、長文入力時に生成が途中で打ち切られないことの確認
- 用途・推敲レベルの組み合わせ（例: 「大学レポート」×「しっかり修正」）による出力差の確認
- サーバーログ（`Ollama raw response: ...`）に生のレスポンスが出力され、デバッグに利用できることの確認

## 5. 既知の変更点（旧仕様書との差分）
- LLM接続ライブラリを `openai`パッケージ（OpenAI互換API経由） から **`ollama`パッケージ（純正SDK）** に変更
  - 理由：OpenAI互換API経由では `think`（思考モード無効化）が正しく反映されないケースがあり、モデルの思考過程がそのまま `processed_text` に出力される不具合が確認されたため
- `think=False` の指定により、AIの思考（reasoning）過程を出力させないテスト項目（No.13）を追加
- システムプロンプト修正により、指示文の復唱を防止するテスト項目（No.14, 15）を追加
- `content` が空だった場合の `thinking` フィールドへのフォールバック処理に関するテスト項目（No.16）を追加
- `context` フィールド・関西弁ボタン・OpenRouter APIキー関連のテスト項目は現行実装に存在しないため引き続き削除対象

---