# テスト仕様書: Flask + Ollama 文章推敲アプリ

## 1. テスト対象
- `app.py`（Flaskサーバー/APIバックエンド）
- `static/index.html`（フロントエンドUI、Vue.js）

## 2. テスト環境
- Windows 10/11
- Python 3.13
- Flask 3.1.3, openai 2.46.0（Ollamaへのクライアントとして使用）
- Ollama（ローカル起動、モデル: `qwen3.5:0.8b`）
- ブラウザ: Chrome, Edge, Firefox

### 事前準備
- `ollama serve` が起動していること
- `ollama pull qwen3.5:0.8b` でモデルを取得済みであること
- `pip install -r requirements.txt` を実行済みであること

## 3. テスト項目

### 3.1. サーバー起動・静的ファイル配信

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 1  | サーバー起動 | `python app.py` で起動 | エラーなく起動し、`http://localhost:5000/` でアクセス可能 |
| 2  | index.html配信 | ブラウザで `/` にアクセス | Vue.js UI（用途選択・推敲レベル選択・入力欄・ボタン）が表示される |

### 3.2. UI操作 - 用途選択

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 3  | 大学レポート選択 | 用途プルダウンで「大学レポート」を選択し文章を送信 | 「です・ます調」等の混在がなく、レポート調に整えられた文章が返る |
| 4  | メール選択 | 用途プルダウンで「メール」を選択し文章を送信 | 相手に失礼のない、自然なメール調の文章が返る |
| 5  | 就活・ES選択 | 用途プルダウンで「就活・ES」を選択し文章を送信 | 簡潔で具体性のある、誇張を抑えた文章が返る |
| 6  | 一般文章選択 | 用途プルダウンで「一般文章」を選択し文章を送信 | 誤字脱字・不自然な表現が修正された文章が返る |

### 3.3. UI操作 - 推敲レベル選択

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 7  | 軽く修正 | 推敲レベルで「軽く修正」を選択し送信 | 原文の構成を保ったまま、誤字脱字レベルの最小限の修正のみ行われる |
| 8  | 自然に修正 | 推敲レベルで「自然に修正」を選択し送信 | 原文を活かしつつ、自然で分かりやすい文章に修正される |
| 9  | しっかり修正 | 推敲レベルで「しっかり修正」を選択し送信 | 文章構成・語順まで積極的に修正されるが、意味・事実は変わらない |

### 3.4. /send_api API 正常系

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 10 | テキスト送信 | `{"text": "...", "purpose": "report", "level": "normal"}` を送信 | 200が返り、`processed_text` に推敲結果（推敲後の文章／主な修正点／文章評価を含む）が含まれる |
| 11 | purpose未指定 | `purpose` を省略して送信 | デフォルト値 `normal` として処理される |
| 12 | level未指定 | `level` を省略して送信 | デフォルト値 `normal` として処理される |

### 3.5. /send_api API 異常系

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 13 | 空文字送信 | `text` を空文字（または空白のみ）で送信 | 400エラー、`{"error": "文章を入力してください。"}` |
| 14 | textフィールドなし | `text` キーなしでAPI送信 | 400エラー、`{"error": "文章が送信されていません。"}` |
| 15 | AI応答なし | Ollamaからのレスポンスが空 | `processed_text` に「AIから有効な応答がありませんでした。」が返る |
| 16 | Ollama未起動 | `ollama serve` を停止した状態でAPIを実行 | 500エラー、`{"error": "AIサービスとの通信中にエラーが発生しました。"}` |

### 3.6. UIテスト

| No | テスト内容 | 手順 | 期待結果 |
|----|------------|------|----------|
| 17 | 文字数カウント | テキストエリアに入力 | 入力欄下部に「◯◯文字」が正しく表示される |
| 18 | 実行中の表示 | 「AIで推敲する」ボタン押下直後 | ボタンが非活性になり、「推敲中...」表示になる |
| 19 | 結果表示 | 応答が正常時 | 「推敲結果」見出しの下、カード内に応答内容が表示される |
| 20 | エラー表示 | エラー発生時 | 赤色のアラート欄にエラーメッセージが表示される |
| 21 | クリアボタン | 「クリア」ボタン押下 | 入力欄・結果欄・エラー欄がすべて空になる |
| 22 | 未入力での実行 | 入力欄が空の状態で「AIで推敲する」を押下 | API送信されず、「文章を入力してください。」が表示される |

## 4. その他確認事項
- 入力最大長、応答最大長の確認
- 日本語以外の入力（英語・中国語等）に対する挙動確認
- レスポンス速度の目安計測（`qwen3.5:0.8b` はローカル低負荷モデルのため、応答内容の精度も併せて確認）
- 用途・推敲レベルの組み合わせ（例: 「大学レポート」×「しっかり修正」）による出力差の確認

## 5. 既知の変更点（旧仕様書との差分）
- OpenRouter APIからローカルOllama（`qwen3.5:0.8b`）利用に変更されたため、APIキー関連のテスト項目（旧No.8）は削除
- `context` フィールド・関西弁ボタン（旧No.4, 5）は現行UIに存在しないため削除
- 代わりに「用途（purpose）」「推敲レベル（level）」の選択機能に関するテスト項目を追加

---
