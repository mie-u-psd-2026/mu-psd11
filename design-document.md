# Webアプリ仕様書

## 1. 概要

ユーザーが入力した日本語の文章を、選択した「用途」と「推敲レベル」に応じて、
LLM（Ollamaでローカル実行）が推敲し、修正後の文章・主な修正点・文章評価を
表示するアプリ。

## 2. 主な機能

### 2.1 入出力フォーム機能
- 用途プルダウンでユーザーが文章の用途を選択する
  - 大学レポート（`report`）
  - メール（`email`）
  - 就活・ES（`es`）
  - 一般文章（`normal`）
- 推敲レベルプルダウンでユーザーが修正の強さを選択する
  - 軽く修正（`light`）
  - 自然に修正（`normal`）
  - しっかり修正（`strong`）
- テキストエリアでユーザーが推敲したい文章を入力する（入力文字数を表示）
- 「AIで推敲する」ボタン押下で 2.2 生成AIリクエストAPIを実行し、応答を取得する
- 「クリア」ボタン押下で入力欄・結果欄・エラー欄をリセットする
- 同一画面上に推敲結果を表示する
- 通信エラー・入力エラー時はアラート欄にエラーメッセージを表示する

### 2.2 生成AIリクエストAPI（`POST /send_api`）
- リクエストボディ: `{ "text": string, "purpose": string, "level": string }`
- `purpose` と `level` の値に応じて、あらかじめ定義された定型プロンプト（用途別指示・レベル別指示）を組み合わせ、システムプロンプトを生成する
- システムプロンプトには、指示文や見出しの説明文自体を出力に含めないよう明示的に指示している（AIが指示文をそのまま復唱してしまう問題への対策）
- システムプロンプトと共に、ユーザーが入力した文章をLLMへ送信する
- ローカル起動のOllamaへ、**Ollama純正のPython SDK（`ollama`パッケージ）** を用いてリクエストする
  - `think=False` を指定し、モデルの思考（reasoning）過程を出力させず、最終回答のみを生成させる
  - `options.num_ctx` を拡大し（8192）、長い出力の途中でコンテキスト長超過により打ち切られることを防ぐ
- LLMの応答は以下の形式で返るよう指示している
  - 推敲後の文章
  - 主な修正点（最大5個、修正前→修正後、理由）
  - 文章評価（読みやすさ／簡潔さ／自然さの3項目を100点満点で採点）
  - 総評（文章全体についての短いコメント）
- 応答の `content` が空の場合、`thinking` フィールドの内容があればフォールバックとして表示し、両方とも空の場合は「AIから有効な応答がありませんでした。」を返す
- 応答をJSON形式（`{ "message": string, "processed_text": string }`）でフロントエンドに返す
- 入力エラー（文章未入力等）は400、AI通信エラーは500でエラーメッセージを返す

## 3. 使用技術

| 分類         | 技術・ライブラリ |
|--------------|------------------|
| フロントエンド | Vue.js 3（CDN版）/ HTML5 + Bootstrap 5 |
| バックエンド  | Flask |
| 通信方式     | Fetch API / JSON |
| LLMモデル    | Ollama（ローカル起動、`qwen3.5:0.8b`） |
| LLM接続方式  | Ollama純正Python SDK（`ollama`パッケージ、`think=False`指定） |

## 4. 入出力例

### 入力
- 用途：大学レポート
- 推敲レベル：自然に修正
- ユーザー入力：「このレポートでは、AIについて書きます。AIはこれからの社会にとってとても重要だと思うし、いろんな分野で使われていくと思います。」

### 出力例

```
【推敲後の文章】

本研究における AI に関する見解を述べる。AI は将来の社会において
極めて重要な役割を果たすと考える。幅広い分野での応用が期待される。

【主な修正点】

1. 「このレポートでは」
   → 「本研究における」
理由：レポートとしてより客観的な導入表現にするため。

2. 「書きます」
   → 「述べる」
理由：口語的な表現を避け、レポートに適した文体にするため。

3. 「とても重要だと思うし」
   → 「極めて重要な役割を果たすと考える」
理由：「です・ます調」と「だ・である調」の混在を解消し、
      断定的で客観的な表現にするため。

【文章評価】

読みやすさ：92点／100点
簡潔さ：95点／100点
自然さ：90点／100点

【総評】

口語的な表現が整理され、レポートとしての客観性・簡潔さが向上した。
```

## 5. 既知の変更点（旧仕様書との差分）
- LLM接続先を OpenRouter経由の各種LLM から、ローカル起動のOllama（`qwen3.5:0.8b`）に変更
- LLM接続に使用するライブラリを OpenAI互換クライアント（`openai`パッケージ） から **Ollama純正SDK（`ollama`パッケージ）** に変更
  - 理由：OpenAI互換API経由では `think`（思考モード無効化）パラメータが正しく反映されないケースがあり、モデルの思考過程がそのまま応答として返ってしまう不具合があったため
- 自由入力の「コンテキスト」欄は廃止し、「用途」「推敲レベル」のプルダウン選択に変更
- 出力形式を汎用的な回答表示から、「推敲後の文章／主な修正点／文章評価／総評」の4部構成に変更
- システムプロンプトに、指示文自体を出力に含めないよう明示する注意書きを追加（AIが指示文を復唱してしまう不具合への対策）
- フロントエンドのCSSフレームワークとしてBootstrap 5を採用